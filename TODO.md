# TODO: Fix FoodDetails image error

1. ✅ Create TODO.md with steps
2. ✅ Import hardcoded `product` from FoodDetails.tsx into Shop/page.tsx
3. ✅ Pass `product` prop to `<FoodDetails />` component
4. Test the fix by running the dev server
